% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restore_and_update.R
\name{renvvv_restore_and_update}
\alias{renvvv_restore_and_update}
\alias{renv3_restore_and_update}
\title{Restore and Update renv Lockfile Packages}
\usage{
renvvv_restore_and_update(
  github = TRUE,
  non_github = TRUE,
  biocmanager_install = FALSE,
  skip = character(0),
  skip_if_dep_unavailable = TRUE,
  prompt = FALSE,
  transactional = FALSE,
  args_restore = list(),
  args_update = list(),
  args_install = list()
)
}
\arguments{
\item{github}{Logical. Whether to process GitHub packages. Default is \code{TRUE}.}

\item{non_github}{Logical. Whether to process non-GitHub packages
(CRAN and Bioconductor). Default is \code{TRUE}.}

\item{biocmanager_install}{Logical.
If \code{TRUE}, Bioconductor packages will be installed using
\code{BiocManager::install}; otherwise,
\code{renv::install("bioc::<package_name>")} will be used.
Default is \code{FALSE}.}

\item{skip}{Character vector. Package names to skip during restore and update.
Default is \code{character(0)} (no packages skipped).}

\item{skip_if_dep_unavailable}{Logical. Passed to \code{renvvv_update()}. If
\code{TRUE}, skip installing a package during the update step when one of its
lockfile-listed dependencies previously failed to install and is not
currently available. Default is \code{TRUE}.}

\item{prompt}{Logical. Whether to prompt for user confirmation during renv
operations. Default is \code{FALSE}.}

\item{transactional}{Logical. Whether to use transactional package
restoration. Default is \code{FALSE}.}

\item{args_restore}{List. Arbitrary arguments to pass down to \code{renv::restore()}.}

\item{args_update}{List. Arbitrary arguments to pass down for update operations.}

\item{args_install}{List. Arbitrary arguments to pass down to \code{renv::install()}.}
}
\value{
Invisibly returns \code{TRUE} upon successful completion.
}
\description{
First restores packages from the lockfile, then updates them to the latest
versions. Combines \code{renvvv_restore()} and \code{renvvv_update()} in sequence.

If an renv project is detected (via \code{renv.lock} file) but not currently
active, the function will activate it. In interactive sessions, the user
will be prompted for confirmation before activation. In non-interactive
sessions, activation occurs automatically.
}
